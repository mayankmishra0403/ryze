# RYZE — production nginx config with HTTPS (Let's Encrypt).
# Replaces the HTTP-only nginx.conf when deploying with docker-compose.ssl.yml.
# Replace api.oppertunity.xyz with your real domain (e.g. sed -i 's/api.oppertunity.xyz/app.ryze.dev/g').

server {
    listen 80;
    server_name api.oppertunity.xyz;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name api.oppertunity.xyz;

    ssl_certificate /etc/letsencrypt/live/api.oppertunity.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.oppertunity.xyz/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:SSL:10m;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;

    # SPA shell must always be revalidated so new bundles ship immediately.
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        expires -1;
    }

    # Hashed build assets are immutable — safe to cache for a year.
    location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|webp|ico|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /api/ {
        proxy_pass http://server:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://server:5001;
        proxy_set_header Host $host;
    }

    location /ws {
        proxy_pass http://server:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
