# RYZE — nginx reverse proxy + static frontend
# Serves the React build, proxies /api and /ws to the Express server.

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;

    # API
    location /api/ {
        proxy_pass http://server:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Uploaded files (avatars, notes, resumes)
    location /uploads/ {
        proxy_pass http://server:5001;
        proxy_set_header Host $host;
    }

    # Socket.io (WebSocket upgrade)
    location /ws {
        proxy_pass http://server:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS: add a Let's Encrypt cert with certbot (e.g. `certbot --nginx -d app.ryze.dev`)
# then uncomment and use:
#
# server {
#     listen 443 ssl;
#     server_name app.ryze.dev;
#     ssl_certificate /etc/letsencrypt/live/app.ryze.dev/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/app.ryze.dev/privkey.pem;
#     # ... same location blocks as above ...
# }
